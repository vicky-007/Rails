<h1>Blog Posts<h1>

<% @post.each do |post| %>
    <div class="card card-body bg-light d-block">
        <h3> <%= post.title %> </h3>
        <h6> <%= post.body %> </h6>

        <%= link_to "Read more", post_path(post), :class => 'btn btn-primary' %>
        
    </div>
    <br>
<% end %>